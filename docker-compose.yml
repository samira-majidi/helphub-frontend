services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: helphub-frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    extra_hosts:
      - "host.docker.internal:host-gateway"
